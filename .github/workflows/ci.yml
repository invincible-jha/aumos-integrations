# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 MuVeraAI Corporation
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  fire-line:
    name: Fire line audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan for forbidden identifiers
        run: bash scripts/fire-line-audit.sh

  python:
    name: Python (${{ matrix.python-version }}) — ${{ matrix.package }}
    runs-on: ubuntu-latest
    needs: fire-line
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
        package:
          - packages/langchain
          - packages/crewai
          - packages/autogen
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install package with dev dependencies
        working-directory: ${{ matrix.package }}
        run: pip install -e ".[dev]"
      - name: Lint with ruff
        working-directory: ${{ matrix.package }}
        run: ruff check src/
      - name: Type check with mypy
        working-directory: ${{ matrix.package }}
        run: mypy src/
      - name: Run tests
        working-directory: ${{ matrix.package }}
        run: pytest

  typescript:
    name: TypeScript — openclaw (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    needs: fire-line
    strategy:
      matrix:
        node-version: [20, 22]
    defaults:
      run:
        working-directory: packages/openclaw
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: packages/openclaw/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Type check
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Build
        run: npm run build
      - name: Test
        run: npm test
